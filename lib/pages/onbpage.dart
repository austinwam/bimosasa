import 'package:flutter/material.dart';

class Onboudingpage extends StatelessWidget {
  const Onboudingpage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("onbouding")),
    );
  }
}
